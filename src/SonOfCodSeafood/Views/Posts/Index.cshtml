@model IEnumerable<SonOfCodSeafood.Models.Post>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Son of Cod News!</h2>

<div class="col-md-12">
@if (Model.Any())
{
    @foreach (var post in Model)
    {
        <h2>Hi</h2>
        @*<h2>@Html.DisplayNameFor(modelPost => post.Title)</h2>
        <h4>@Html.DisplayFor(postItem => post.Title)</h4>*@
    }
}
</div>
else
{
<p>No posts have been added yet!</p>
}

@Html.ActionLink("Create new", "Create", "Posts")